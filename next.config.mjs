/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
      // Remove any experimental features that are no longer supported
      serverActions: true,
    },
    // Add any other necessary configuration options here
  }
  
  module.exports = nextConfig
  